---
import MaximizeImage from "../MaximizeImage.astro";

interface Props {
  src: string;
  alt?: string;
}

const { src, alt = "", ...rest } = Astro.props;
---

<MaximizeImage src={src} alt={alt} {...rest} />
