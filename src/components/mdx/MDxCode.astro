---

---

<style>
  code {
    position: relative;
    z-index: 0;
    margin: 0.125rem 0.625rem 0.125rem 0.5rem;

    &::before {
      content: "";
      position: absolute;
      inset: 0;
      width: calc(100% + 0.5rem);
      height: calc(100% + 0.125rem);
      margin: -0.125rem -0.375rem;
      background-color: var(--semantic-surface);
      border: 0.5px solid var(--semantic-border-high);
      border-radius: var(--radii-xsmall);
      pointer-events: none;
      z-index: -1;
    }
  }
</style>

<!-- Falta estilizar este componente -->
<code class="body-normal bold color-secondary">
  <slot />
</code>
